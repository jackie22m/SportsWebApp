info:
  name: Get athlete by location
  type: http
  seq: 8

http:
  method: GET
  url: http://localhost:9926/athleteProfiles/?location=searcy
  params:
    - name: location
      value: searcy
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
