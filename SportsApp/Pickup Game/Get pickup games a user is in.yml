info:
  name: Get pickup games a user is in
  type: http
  seq: 11

http:
  method: GET
  url: http://localhost:9926/api/pickupGames/me
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
