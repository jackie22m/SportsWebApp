info:
  name: Join a pickup game
  type: http
  seq: 12

http:
  method: POST
  url: http://localhost:9926/api/pickupGames/join/019de6a5-2ccb-70eb-af3d-389cfe42d693
  body:
    type: json
    data: |
      {
        "status":"joined"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
