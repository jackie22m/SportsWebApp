info:
  name: Get all pickup games
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:9926/api/pickupGames
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
