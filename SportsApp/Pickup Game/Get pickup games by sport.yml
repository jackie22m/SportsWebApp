info:
  name: Get pickup games by sport
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:9926/pickupGames?sport=Soccer
  params:
    - name: sport
      value: Soccer
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
