info:
  name: Get the games a user is in
  type: http
  seq: 47

http:
  method: GET
  url: http://localhost:9926/api/users/me/games
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
