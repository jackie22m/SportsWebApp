info:
  name: List upcoming games
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:9926/pickupGames
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
