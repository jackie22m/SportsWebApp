info:
  name: joinPickupGame
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:9926/pickupGames/1/join
  body:
    type: json
    data: |-
      {
        "userId": "2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
