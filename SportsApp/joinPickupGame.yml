info:
  name: joinPickupGame
  type: http
  seq: 7

http:
  method: POST
  url: /pickupGames/2/1
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
