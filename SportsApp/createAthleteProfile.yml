info:
  name: createAthleteProfile
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:9926/athleteProfiles
  body:
    type: json
    data: |-
      {
        "bio": "Hi",
        "primarySport": "soccer", 
        "seondarySport": "basketball",
        "position": "goalkeeper",
        "skillLevel": "Professional"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
