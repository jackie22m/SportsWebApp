info:
  name: Sign Up
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:9926/signup
  body:
    type: json
    data: |-
      {
        "name": "Jackie Martinez",
        "email": "jackie@example.com",
        "password": "mypassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
