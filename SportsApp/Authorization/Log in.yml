info:
  name: Log in
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:9926/api/login
  body:
    type: json
    data: |-
      {
        "email":"jackie123@example.com",
        "password": "jackie123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
