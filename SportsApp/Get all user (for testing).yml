info:
  name: Get all user (for testing)
  type: http
  seq: 14

http:
  method: GET
  url: http://localhost:9926/users
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
