info:
  name: createUser
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:9926/users
  body:
    type: json
    data: |-
      {
        "name": "Jackie Martinez",
        "displayName": "Jackie",
        "email": "jackie@example.com",
        "password": "mypassword123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
